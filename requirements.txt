# ── Streamlit 前端依赖 ──────────────────────────────────────────────────────────
# fastapi / uvicorn / playwright 是后端服务专用，Streamlit Cloud 不需要也不支持

streamlit>=1.35.0
openai>=1.30.0
httpx>=0.27.0
python-docx>=1.1.0
pypdf>=4.0.0
